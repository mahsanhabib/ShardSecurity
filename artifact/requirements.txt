# Artifact dependencies for "Priced to Equivocate".
# Tested with Python 3.12.  Pin-compatible with the versions below.
numpy>=1.24,<3
matplotlib>=3.7,<4
pyyaml>=6.0,<7
cryptography>=41.0          # real Ed25519 signatures in the discrete-event testbed
# (pytest is optional; the test suite also runs as a plain script.)
pytest>=7.0 ; extra == "dev"
