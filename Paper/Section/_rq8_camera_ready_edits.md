# RQ8 camera-ready edits — APPLY ONLY AFTER the measurement is executed

> ⚠ **CAUTION ADDED 2026-06-14 — do not overclaim `q_max`.** The executed Cosmos arm
> measured `q_max ≈ 0.97`, but this is **enforcement conditional on a slash record
> existing** — a near-tautological / selection-biased *upper bound*, NOT the protocol's
> true enforcement probability (un-pursued equivocations leave no trace). Do **not** write
> "we measure q_max = 0.97, therefore Cosmos is safe." The defensible claims are the
> *qualitative* findings (deadline = evidence-expiry not unbonding; 48h is not a hard
> cutoff; enforcement within ~1–2 blocks) and the *structural* RULED_OUT (finality-gating).
> `T_acc` is block-resolution/clock-skew bounded — report "within ~1–2 blocks," not
> sub-second. The "we now measure it" target text below assumes the FULL protocol ran
> (bridge + controls + adversarial T_acc + self-run-archive re-derivation); soften it to
> match whatever was actually executed.

These rewrites assert that $q$, $T_{\text{acc}}$, $T_{\text{settle}}$ were **measured**. Do **not**
apply them to the live paper until `prereg/rq8-v1` has been run and the measured macros in
`Paper/Section/_generated/` carry real numbers (not `[pending]`). Until then, the honest in-tree state is:
the protocol section `measured_evaluation.tex` (framed as a pre-registered plan) + the updated
Limitation (3) pointer. This file holds the *post-results* target text.

---

## 1. `Paper/Section/abstract.tex` — replace the case-study sentence

**Current (keep until results exist):**
> A case study on the documented parameters of six deployed systems---not a live exploit claim---shows
> that long unbonding periods ($q\to1$) and finality-gated settlement place them in the safe regime,
> concentrating the residual risk in fast-exit, ungated-settlement designs.

**Target (after measurement):**
> We then \emph{measure} the load-bearing quantities under a pre-registered protocol: on Cosmos
> Hub/CometBFT we measure $q$ (against the true evidence-expiry deadline, not unbonding) and $\Tacc$
> with both a cooperative lower bound and an adversarial evidence-suppression upper bound, and the
> same instrument that returns a falsifiable \textsc{ruled-out} on finality-gated IBC settlement
> returns a measured \textsc{open-window} ($\Tsettle\!\approx$ seconds $\ll\Tacc$) on a real
> fast-intent-bridge value path---for which we re-derive the economic gate under solver-borne loss and
> isolate when the equivocation primitive is load-bearing versus a plain-reorg griefing distinct from
> prior bridge work. We report, per deployed design, the exact settlement-speed cliff that flips it
> from safe to exposed.

## 2. `Paper/Section/introduction.tex` — add a contribution bullet (after the reproducible-artifact bullet)

> \item \textbf{A pre-registered live-measurement of the load-bearing quantities}
> (\S\ref{sec:measured}): we measure $q$, $\Tacc$, and $\Tsettle$ on a real finality-gated path
> (Cosmos/IBC, \textsc{ruled-out}) and a real fast-settlement path (an intent bridge,
> \textsc{open-window}) with one instrument and two calibrated controls, evaluate each verdict against
> the bound working against it, and give every deployed design a distance-to-cliff. This discharges
> the load-bearing-$q$ limitation: the safe/exposed dichotomy is measured, not assumed.

## 3. `Paper/Section/limitations.tex` (3) — replace the §\ref{sec:measured} pointer with the executed result

**Target (after measurement):**
> \emph{(3) $q$ is load-bearing; we now measure it.} \S\ref{sec:measured} reports measured $q$
> (against the evidence-expiry deadline), $\Tacc$ (cooperative and adversarial bounds), and
> $\Tsettle$ on Cosmos/CometBFT (\textsc{ruled-out}, against the adversarial $\Tacc$) and a real
> fast-intent-bridge path (\textsc{open-window}), plus a low-$q$ measurement on a fast-exit control
> and a per-system distance-to-cliff. The residual limitation is that the worst-case ungated design
> space is represented by a constructed control rather than a mature deployment, and that
> $q_{\mathrm{attack}}$ is measured on private devnets rather than a production validator set.

## 4. `Paper/Section/conclusion.tex` — one sentence

> Beyond the model, we pre-register and execute a measurement that turns the safe/exposed dichotomy
> into an instrument-calibrated fact and gives each deployed design a settlement-speed cliff distance.

## 5. Mechanics
- After `python run_all.py --config configs/main.yaml` with `results/measured/*.jsonl` present, the
  measured macros (`\ArtifactQMaxCosmos`, `\ArtifactCosmosVerdict`, `\ArtifactBridgePwin`,
  `\ArtifactBridgeVerdict`, `\ArtifactCliffCosmos`, …) and `tab_measured.tex` are regenerated by
  `make_tables.py`; `measured_evaluation.tex` then shows real numbers and the table appears
  automatically (`\IfFileExists`).
- Remove the `\providecommand` placeholder block at the top of `measured_evaluation.tex` only once
  the generated macros are guaranteed present in every build.
